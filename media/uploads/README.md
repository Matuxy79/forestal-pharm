# Uploaded media

Files here are created automatically by the running app whenever someone attaches an image or
video in the chat. Each is prefixed with a `YYYYMMDD-HHMMSS-` timestamp plus a slugified version
of the original filename. They're real, permanent copies on disk — safe to rename, move, or delete
by hand to curate the final set of shop media.
